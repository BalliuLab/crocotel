% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_expression.R
\name{simulate_expression}
\alias{simulate_expression}
\title{Simulate multi-context regulator and target gene expression}
\usage{
simulate_expression(
  n_targets,
  n_contexts = 20,
  n_individuals,
  n_snps = 500,
  maf_min = 0.05,
  maf_max = 0.5,
  h2_sh_reg = 0.3,
  h2_sp_reg = 0.1,
  rho_E_reg = 0.4,
  k_sh_reg = 5L,
  k_sp_reg = 3L,
  k_pure_sp_reg = 0L,
  pi_C_reg = 1,
  h2_sh_tgt = 0.3,
  h2_sp_tgt = 0.1,
  k_sh_tgt = 5L,
  k_sp_tgt = 3L,
  k_pure_sp_tgt = 0L,
  pi_C_tgt = 1,
  h2_Y = 0.2,
  n_active_contexts = 1L,
  rho_E_tgt = 0.4,
  frac_true_targets = 1,
  n_chrs_reg = 11L,
  n_chrs_tgt = 11L,
  seed = NULL
)
}
\arguments{
\item{n_targets}{Integer. Number of regulator-target pairs T.}

\item{n_contexts}{Integer. Number of contexts C. Default 20.}

\item{n_individuals}{Integer. Number of individuals I.}

\item{n_snps}{Integer. cis-SNPs per gene. Default 500.}

\item{maf_min}{Numeric. Minimum MAF. Default 0.05.}

\item{maf_max}{Numeric. Maximum MAF. Default 0.50.}

\item{h2_sh_reg}{Numeric. Shared cis heritability of regulator. Default 0.3.}

\item{h2_sp_reg}{Numeric. Specific cis heritability of regulator. Default 0.1.}

\item{rho_E_reg}{Numeric. Residual correlation for regulator. Default 0.4.}

\item{k_sh_reg}{Integer. Causal shared SNPs for regulator. Default 5.}

\item{k_sp_reg}{Integer. Causal specific SNPs for regulator. Default 3.}

\item{k_pure_sp_reg}{Integer. Pure specific SNPs for regulator. Default 0.}

\item{pi_C_reg}{Numeric. Proportion of specific contexts for regulator. Default 1.0.}

\item{h2_sh_tgt}{Numeric. Shared cis heritability of target. Default 0.3.}

\item{h2_sp_tgt}{Numeric. Specific cis heritability of target. Default 0.1.}

\item{k_sh_tgt}{Integer. Causal shared SNPs for target. Default 5.}

\item{k_sp_tgt}{Integer. Causal specific SNPs for target. Default 3.}

\item{k_pure_sp_tgt}{Integer. Pure specific SNPs for target. Default 0.}

\item{pi_C_tgt}{Numeric. Proportion of specific contexts for target. Default 1.0.}

\item{h2_Y}{Numeric. Trans heritability from regulator GReX. Default 0.2.}

\item{n_active_contexts}{Integer >= 1. Number of contexts in which each
regulator-target pair carries the trans signal (a count, not a
fraction; must not exceed \code{n_contexts}). Default \code{1L}.}

\item{rho_E_tgt}{Numeric. Target residual correlation across contexts.
Default 0.4. Same role as \code{rho_E_reg} but for the target side.}

\item{frac_true_targets}{Numeric in [0, 1]. Fraction of targets that
carry a true trans effect; the rest are null at the eTarget level.
Default 1.0 (every target is true). Use < 1 to plant null targets for
testing eTarget-level FDR control (e.g. via treeQTL).}

\item{n_chrs_reg}{Integer. Number of chromosomes regulators are spread
across, round-robin over chrs 1..n_chrs_reg. Default 11.}

\item{n_chrs_tgt}{Integer. Number of chromosomes targets are spread
across, round-robin over the block above the regulators,
(n_chrs_reg+1)..(n_chrs_reg+n_chrs_tgt). Default 11. The two disjoint
blocks make every (regulator, target) pair cross-chromosome by
construction; set both to 1 for the all-regs-chr1 / all-tgts-chr2 layout.}

\item{seed}{Integer or NULL. Master random seed. \code{NULL} (default) leaves the RNG
untouched: each call draws fresh randomness, so replicate loops give
independent datasets. Pass a seed for a reproducible dataset -- and
give each replicate its OWN seed (a fixed shared seed would make
every replicate identical).}
}
\value{
Named list:
\describe{
  \item{regulator}{Output of \code{simulate_regulator_expression()} for
    regulator genes.}
  \item{target_cis}{Output of \code{simulate_regulator_expression()} for
    target genes. Contains the true target cis GReX in \code{$GReX_std}.}
  \item{target}{Output of \code{simulate_target_expression()}.}
  \item{G_list_reg}{List of regulator genotype matrices (length n_targets).}
  \item{G_list_tgt}{List of target genotype matrices (length n_targets).}
  \item{chr_per_reg}{Integer vector: chromosome of each regulator,
    round-robin over chrs 1..n_chrs_reg.}
  \item{chr_per_tgt}{Integer vector: chromosome of each target,
    round-robin over chrs (n_chrs_reg+1)..(n_chrs_reg+n_chrs_tgt).}
  \item{params}{Named list of all top-level parameters.}
}
}
\description{
Generates n_targets pairs of (regulator, target) genes, each with its
own independent cis-genotype matrix. Regulator $t$ drives target $t$
(1:1 pairing).
}
\examples{
sim <- simulate_expression(
  n_targets       = 3,
  n_contexts      = 20,
  n_individuals   = 500,
  n_snps          = 500,
  h2_sh_reg = 0.3, h2_sp_reg = 0.1, rho_E_reg = 0.4,
  h2_sh_tgt = 0.3, h2_sp_tgt = 0.1,
  h2_Y = 0.2, n_active_contexts = 4, rho_E_tgt = 0.4,
  seed = 1
)
dim(sim$regulator$E)     # 500 x 3 x 20
dim(sim$target$Y)        # 500 x 3 x 20
dim(sim$target_cis$GReX) # 500 x 3 x 20
}
