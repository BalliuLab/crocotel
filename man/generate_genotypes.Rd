% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_genotypes.R
\name{generate_genotypes}
\alias{generate_genotypes}
\title{Generate a raw cis-genotype dosage matrix}
\usage{
generate_genotypes(
  n_individuals,
  n_snps = 500,
  maf_min = 0.05,
  maf_max = 0.5,
  seed = NULL
)
}
\arguments{
\item{n_individuals}{Integer. Number of individuals (I).}

\item{n_snps}{Integer. Number of cis-SNPs (P). Default 500.}

\item{maf_min}{Numeric. Minimum minor-allele frequency. Default 0.05.}

\item{maf_max}{Numeric. Maximum minor-allele frequency. Default 0.50.}

\item{seed}{Integer or NULL. Random seed.}
}
\value{
Numeric matrix (n_individuals x n_snps) of raw 0/1/2 dosages,
  with attributes:
  \describe{
    \item{maf}{Numeric vector of per-SNP MAFs.}
    \item{maf_range}{Numeric vector c(maf_min, maf_max).}
    \item{source}{Character: "simulated".}
  }
}
\description{
Simulates SNP genotypes under Hardy-Weinberg equilibrium for a single
genomic locus (e.g. the cis-window of one regulator gene). The matrix
contains raw integer dosages \{0, 1, 2\}; downstream simulation and
fitting code standardises columns when needed.
}
\details{
The returned format matches \code{load_genotypes()} so that downstream
functions are agnostic to whether genotypes are simulated or real.
}
\examples{
G <- generate_genotypes(n_individuals = 300, n_snps = 200, seed = 1)
dim(G)              # 300 x 200
all(G \%in\% 0:2)     # TRUE
colMeans(G) / 2     # ~maf for every SNP
}
